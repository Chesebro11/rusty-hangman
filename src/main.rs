use rusty_hangman::*;

fn main() {
    // I'm not sure if this is even good practice but literally all logic has been moved to the lib.rs
    // file, the onlything that exists here is the main function which calls the game loop.
    start_menu();
}
